; generated by drom from package skeleton 'library'

(library
  (name ocyula_lib)
  (public_name ocyula_lib)
  (wrapped true)
  (libraries  )
  
  
  )

(menhir (modules Parser))
(ocamllex Lexer)

(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ocyula_lib))


